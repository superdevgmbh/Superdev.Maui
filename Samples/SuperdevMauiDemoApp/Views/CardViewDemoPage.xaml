<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    x:Class="SuperdevMauiDemoApp.Views.CardViewDemoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:SuperdevMauiDemoApp.Controls"
    xmlns:s="http://schemas.superdev.ch/dotnet/2021/maui"
    xmlns:vm="clr-namespace:SuperdevMauiDemoApp.ViewModels"
    x:DataType="vm:CardViewDemoViewModel"
    Title="CardViewDemoPage">

    <Grid>
        <ScrollView>
            <VerticalStackLayout
                Padding="{DynamicResource Theme.Padding.CardPadding}"
                Spacing="{DynamicResource Theme.Spacing.Medium}">
                <Label
                    Style="{StaticResource DemoLabelStyle}"
                    Text="Demo: AddressControl uses CardView" />
                <controls:AddressControl BindingContext="{Binding .}" />

                <Label
                    Style="{StaticResource DemoLabelStyle}"
                    Text="Demo: PeriodicTaskView uses CardView" />
                <controls:PeriodicTaskView BindingContext="{Binding PeriodicTask}" />
            </VerticalStackLayout>
        </ScrollView>

        <s:PageActivityIndicator
            Grid.Row="0"
            IsBusy="{Binding IsBusy}"
            Title="{s:Translate LoadingText}" />

        <s:ViewModelErrorControl
            Grid.Row="0"
            ViewModelError="{Binding ViewModelError}" />
    </Grid>
</ContentPage>